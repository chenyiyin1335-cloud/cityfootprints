# 本地开发不需要配置这个文件——Vite 已经在 vite.config.ts 里把 /api 代理到 127.0.0.1:8000。
#
# 只有当前端和后端分开部署（例如前端在 Vercel、后端在 Render）时才需要设置：
# 把下面这一行取消注释，并改成你在 Render 上拿到的后端地址（注意不要带结尾的斜杠 /）
# VITE_API_BASE_URL=https://your-backend.onrender.com
